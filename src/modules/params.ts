export interface Params {
    scene: Phaser.Scene;
    x: number;
    y: number;
    texture: string;
    
}
export interface ParamsExp {
    scene: Phaser.Scene;
    x: number;
    y: number;
    texture: string;
    travelAngle: number;
    
}